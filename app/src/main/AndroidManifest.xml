<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.evo.belezaonline_2">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:name=".Controller.MakerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".UpdateAgendamentoCActivity"></activity>
        <activity android:name=".UpdateServicoActivity" />
        <activity android:name=".ListServicoActivity" />
        <activity android:name=".UpdateFucnActivity" />
        <activity android:name=".ListFunActivity" />
        <activity android:name=".UpdateAgendActivity" />
        <activity android:name=".ListFeitAgdActivity" />
        <activity android:name=".EspeAgendActivity" />
        <activity android:name=".Activis.ListAgendaActivity" />
        <activity android:name=".Cadastros.CadAgenda" />
        <activity android:name=".Activis.CTInfoActivity" />
        <activity android:name=".Controller.VeriCriMapsEmpActivity" />
        <activity android:name=".Controller.VeriCriMapsActivity" />
        <activity android:name=".Cadastros.CadPromocaoActivity" />
        <activity android:name=".Cadastros.CadFuncActivity" />
        <activity android:name=".Cadastros.CadAgendaEmpActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".Maps.MapsActivityEmp"
            android:label="@string/title_activity_maps_emp" />
        <activity android:name=".Cadastros.CadEmpresaServicoActivity" />
        <activity
            android:name=".Maps.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Cadastros.CadEmpresaActivity" />
        <activity android:name=".Cadastros.CadClienteActivity" />
        <activity android:name=".Activis.EsCadActivity" />
        <activity android:name=".Activis.MainActivity" />
        <activity android:name=".Activis.MainActivityEmp" />
        <activity
            android:name=".Activis.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>